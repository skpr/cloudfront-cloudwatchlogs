name: ☁️ Publish Artifacts

on:
  push:
    tags:
      - v1.*

jobs:
  artifacts:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - uses: jdx/mise-action@v4

      - name: lint
        run: mise release
